---
name: xianzhu-skill
description: Use when the user wants to diagnose, stabilize, or improve empirical significance in an economics project under a fixed research question. Trigger whenever the user says things like "试到显著", "换口径", "调参", "试模型", "为什么不显著", "结果方向对但不显著怎么办", or needs a structured search path across x, y, sample, fixed effects, event-study alignment, controls, and admissible specification search. Do not use this skill to invent the research question itself, to fabricate substitute indicators without support, or to hide unsuccessful attempts.
version: 2.0.2
---

# 经济学显著 Skill 2.0.2

## 目标
这个 skill 的目标不是“强行做显著”，而是把一个已经有明确经济学问题的经验结果，放进一套**有顺序、可留痕、可停机、可解释**的规格搜索流程里。

它适合处理三类任务：

1. 用户已经有研究问题，但还没理顺 `x`、`y` 和方法。
2. 用户已经有 `x`、`y`、方程或 do 文件，只想继续往下试。
3. 用户已经跑出结果，但不知道问题出在 `x`、`y`、样本、模型还是控制变量。

## 硬边界

- 不负责发明研究问题本身。
- 不负责在没有老师意见、制度背景或文献支持时，自行创造“理想指标”。
- 不得删去不利结果；只能记录、分类、解释并决定是否采用。
- 不得把“异常短窗口”“异常小样本”或“明显违背经济含义”的显著结果自动升格为正文主规格。
- 同概念替代指标不是第一步，而是当前指标尝试失败后的后续步骤。
- 如果需要具体替代指标，而用户没有提供文献、变量表或既有定义，必须先索要材料。
- 若搜索结果只能得到“边缘显著”或“方向稳定但不显著”，必须如实汇报，不得包装成已经解决。

## 何时不要直接用

以下情形不要直接用本 skill：

- 用户还没确定研究问题，只是说“我想做个有意思的题”。
- 用户需要的是理论机制、文献综述、制度解释或识别策略设计，而不是规格搜索。
- 用户要的是数据清洗、匹配、合并、制图、写作润色，而不是回归结果搜索。
- 用户要求明显不当的 p-hacking，例如“只要显著，不要记录失败规格”。
- 题目本身因法律、制度或伦理约束已经不应继续推进。

若用户还没明确研究问题、变量或识别方式，先让用户补老师意见、目标文献或既有方程，再继续。

## 苏格拉底式提问协议（v2.0.2 起为软纪律）

> 这是本 skill 的"过程内嵌质量门"，与"硬边界""何时不要直接用"互补。
> 前两者是**该不该跑**的问题；这一段是**跑的过程中怎么让自己保持清醒**的问题。

### 为什么需要

跑规格搜索很容易陷入两种陷阱：

1. **机械套用**：每一步按文档操作，但忘了"为什么是这一步"。
2. **结果诱导**：为了得到某个方向/显著性，把每一步的解释都往那个方向拉。

苏格拉底式提问协议的目的，是**让使用者在每一步先回答几个"看起来很基础但经常被跳过"的问题**——

- 既帮助 AI（CC）**显式暴露自己的假设**，避免在没想清楚的前提下硬跑；
- 也帮助使用者**主动梳理自己当前的认识**，避免跑完一长串规格才发现方向走错。

### 协议强度（软纪律）

- 不阻塞执行：每个 Step 之前的 3 个关键问题**标 📝 推荐先问**，但不强制必须回答完才能进入下一步。
- 鼓励留痕：在 `沟通记录.md` / `spec-log` / `step0-audit.md` 里**推荐**追加一段"我刚问了自己什么"。
- 完整清单见 [`references/socratic-prompts.md`](references/socratic-prompts.md) ⭐

### 在哪里问

每个 Step 之前嵌入 **3 个最关键问题**（节选自完整清单）；
SKILL.md 里只列最关键的 3 个，详细问题、追问、对应失败模式请翻完整清单。

### 通用模板

每个问题都遵循 **三段式**：**问题 → 为什么重要 → 怎么答**：

```text
Q: <问题>
   为什么：<跳过它会有什么风险>
   怎么答：<可以怎么回答，答到什么程度算"够">
```

### 与三轮停机的关系

- 三轮停机是**结果异常**触发的硬纪律（同一任务连续 3 次报错 → 强制停机）。
- 苏格拉底式提问是**过程异常**触发的软纪律（每步之前先问几个问题，避免跑偏）。
- 两者不冲突：停机后重新跑，依然要走苏格拉底式提问协议。

## 模式判定
先判断当前任务属于哪一类。

### 模式 A：识别前整理

适用条件：

- 用户只知道要研究 `x` 对 `y` 的影响；
- 或者只知道方向和主题，但还没完全定方程；
- 需要先确认是试 `x`、试 `y`，还是两边都要试。

### 模式 B：已有方程，直接推进

适用条件：

- 用户已经给出 `x`、`y`、模型或现有 do 文件；
- 或者已经有一轮结果，只想继续试到更稳的口径。

如果模式不清楚，先问最少量的问题来锁定：

1. `x` 和 `y` 是否已经被老师、正文或文献明确？
2. 现在有没有现成方程、do 文件或回归结果？
3. 主要是要试 `x`、试 `y`，还是都试？

## 标准工作流

> 📝 **每个 Step 之前先用苏格拉底式提问协议问自己 3 个问题**（见 [`references/socratic-prompts.md`](references/socratic-prompts.md)）。
> 这里只列每个 Step **最关键**的 3 个，详细追问请翻完整清单。

### Step 0. 先判断这个题还值不值得救（v2.0.1 起为硬纪律）

> 📝 推荐先问：
> 1. **这个题目的研究问题本质上要回答什么？用一句话说清楚。**
>    如果说不清楚，问题还没到"值不值得救"这一步，先回去问老师。
> 2. **如果 Step 0 通过但你做不出显著，你打算怎么向老师/审稿人解释？**
>    答不上来 = 你在做"为了显著而显著"的题目。
> 3. **当前数据 / 法律 / 平台是否有任何"已知危险信号"你还没查过？**
>    跳过 L3 任意一项 = 已经在制造未来的返工。
>    完整追问清单见 [`references/socratic-prompts.md`](references/socratic-prompts.md#step-0--题目还能不能做)。

在试显著之前，先**机械跑完 4 层 pre-flight 检查**，任何一层失败都必须先处置，再决定是否进入 7 轮顺序。

**4 层顺序硬纪律**：L1 数据可达 → L2 操作可达 → L3 制度/法律/平台规则 → L4 伦理/风险。**不可乱序、不可跳过、不可合并**。

#### L1 · 数据可达

- 项目根 / `raw/` / `tmp/` 目录存在
- 关键 `.dta` / `.csv` 文件**大小 > 0**
- 用 `mcp__stata-mcp__get_data_info` 或 `pandas.read_stata` 能正确打开
- 行数与既有 `data_manifest.md` / `data_card.md` 一致
- 数据库账号（CSMAR / Wind / CNRDS / RESSET）最近下载距今 ≤ 90 天

**L1 失败 = 直接封存**（写 `STOP_step0_L1.md` + 标 `hard_fail`），不进入 L2。

#### L2 · 操作可达

- Stata 可执行（`which stata` 或 `which StataMP`）
- Stata 许可证有效（`stata -q -e do` 能跑空脚本）
- MCP 通道可连（如用了 `mcp__stata-mcp`）
- Python ≥ 3.10 且 `pandas / numpy / linearmodels / pyfixest / statsmodels` 装好
- 远程计算资源（如 SSH 到学校服务器）可达

**L2 失败 = 直接封存**（写 `STOP_step0_L2.md` + 标 `hard_fail`），不进入 L3。

#### L3 · 制度 / 法律 / 平台规则（允许 `WebSearch` / `WebFetch`）

按"由近到远"逐项检查：

1. **直接相关政策**（中央 / 部委官方文件）：
   - 关键词模板：`<研究主题> + <实施年份> + "政策" OR "通知" OR "办法" OR "意见"`
   - 来源：中国政府网 `https://www.gov.cn/zhengce/`、各部委官网
2. **数据源状态**（CSMAR / Wind / CNRDS / RESSET / CNINFO / HKEX）：
   - 关键词模板：`<数据库名> + "维护" OR "下线" OR "停售" OR "数据更新"`
3. **平台规则**（若用了爬虫 / 开放平台数据：微博 / 知乎 / 小红书 / 抖音 / 快手 / B 站 / 微信公众号 / 百度）：
   - 关键词模板：`<平台名> + "用户协议" OR "开放平台" OR "数据合规"`
4. **法律修订**（《统计法》《数据安全法》《个人信息保护法》《网络安全法》《反垄断法》《公司法》《证券法》）：
   - 关键词模板：`法律名 + "修订" OR "施行" OR "发布"`
   - 来源：国家法律法规数据库 `https://flk.npc.gov.cn/`
5. **学术伦理 / 撤稿**（Retraction Watch、中国科学院文献情报中心）
6. **国际制裁 / 出口管制**（如数据涉及境外实体）

**调用规则**：每条 WebSearch / WebFetch 查询必须包含**时间锚**（年 + 月 + 关键词）；
搜索结果要写进 `step0-audit.md` 的 L3 段，附 5 个以内最权威来源链接。

**L3 冲突候选**（如数据已下线 / 法律禁止 / 方法已被撤稿）= **回写用户，等明确意向**，不擅自决定。

**L3 搜索结果不足** = 进入 L4，**不视为失败**。

#### L4 · 伦理 / 风险（自我审阅 + 回问用户）

红线清单（任一打勾 → 询问用户并要求文字确认）：

- [ ] 题目涉及**特定群体**的歧视性结论（性别 / 民族 / 地区 / 行业 / 信仰）
- [ ] 题目结论若成立，会触发**法律风险**（如证明某群体系统性违规）
- [ ] 数据来源**未经授权**或**明显违规获取**
- [ ] 与**已撤稿 / 已更正**的方法步骤实质重合
- [ ] 题目在**现行法规**下属于禁谈（如非法行业）
- [ ] 题目要求**绕过**已生效的隐私 / 个人信息保护规定

**L4 红线打勾 = 询问用户并要求文字确认**。
用户拒绝 → 封存（同 L1/L2）。
用户继续 → 进入 7 轮顺序，但在 `step0-audit.md` 保留"用户书面确认"快照作为留痕。

#### Step 0 输出：step0-audit.md

每跑一次 Step 0 都要在 `output/审计/step0_<时间戳>.md` 写一份审计报告，包含：

- 项目名 / 时间戳 / 跑 Step 0 的一方 / 最终结果
- L1 / L2 / L3 / L4 四段的"检了什么 + 命令输出 + 结论"
- 决策记录（进入 7 轮顺序 / 封存 / 等用户回话）

#### 何时**重新跑 Step 0**

满足以下任一，重新跑：

- 研究主题变了
- 数据源换了
- 主要被解释 / 处理变量定义换了
- 法律法规 / 平台规则发生变化
- 距上次 Step 0 跑过已经超过 90 天

> 📖 完整机械可执行清单见 [`references/step0-checklist.md`](references/step0-checklist.md)。

### Step 1. 锁定研究问题与 `x/y`

> 📝 推荐先问：
> 1. **你的 x 和 y 在经济理论上分别代表什么因果机制？**
>    答不上来 = 你在做"为跑回归而跑回归"。
> 2. **如果结果方向与你预期相反，你会怎么解释？这种反向解释你能辩护吗？**
>    想不出反向解释 = 你可能在 p-hacking 期望方向。
> 3. **x 和 y 是不是已经被老师 / 正文 / 目标文献明确？**
>    不明确 = 还没到规格搜索，回到问题梳理阶段。
>    完整追问清单见 [`references/socratic-prompts.md`](references/socratic-prompts.md#step-1--锁定研究问题与-xy)。

先确认以下信息是否已经明确：

- 核心问题：`x` 对 `y` 的什么影响？
- 主要解释变量是 `x` 还是其某种代理口径？
- 被解释变量 `y` 是最终结果、机制变量，还是扩展结果？
- 用户是否已经接受某个识别策略或基准模型？

若上述信息未明确，本 skill 只能帮助澄清，不直接开始搜索。

### Step 2. 先审数据，不先审 p 值

> 📝 推荐先问：
> 1. **这一步你打算试 x 还是试 y？为什么不是另一边？**
>    答"两边都要试" = 你还没想清楚要诊断什么。
> 2. **如果两边都要试，你打算先做哪一边？另一边的失败会改变这边的结论吗？**
>    答不上来 = 没控制试错顺序的耦合。
> 3. **检查清单里（覆盖范围 / 缺失 / 时间断点 / 异常分母 / 口径突变 / 行政变化）哪一项最有可能吞掉你的结果？**
>    没思考过 = 默认假设"数据没问题"，这是经验研究最大的坑。
>    完整追问清单见 [`references/socratic-prompts.md`](references/socratic-prompts.md#step-2--先审数据不先审-p-值)。

在开始试规格之前，必须先判断：

- 主要要试的是 `x`
- 主要要试的是 `y`
- `x` 和 `y` 都需要试

同时先检查：

- 覆盖范围
- 缺失位置
- 时间断点
- 异常分母
- 口径突变
- 是否存在法院层级、平台抓取、统计口径或行政区划变化

默认规则：

- 如果经济故事已经稳定，但结果变量口径不稳，优先试 `y`
- 如果结果变量明确、处理变量代理很多，优先试 `x`
- 如果两边都存在多重代理，先选一边做主搜索，另一边后置

### Step 3. 选择估计方法集合

> 📝 推荐先问：
> 1. **你的识别策略要排除的核心混淆是什么？**
>    答不上来 = 选方法等于"猜哪个 R² 高"。
> 2. **选择的方法是否能识别这个混淆？还是你只是因为"它是当前默认方法"而选？**
>    默认方法不等于合适方法。
> 3. **你打算同时上几个方法？如果它们结果不一致，你会怎么选？**
>    答不上来 = 你没想好怎么归因。
>    完整追问清单见 [`references/socratic-prompts.md`](references/socratic-prompts.md#step-3--选择估计方法集合)。

在知道 `x`、`y` 之后，先给出一组合理的估计方法候选，并让用户选择一个或多个。

常见菜单见 `references/method-menu.md`。

原则：

- 先尊重正文已有基准模型
- 只在变量性质确实要求时才换模型
- 不要一上来同时换变量、换模型、换样本，避免无法归因

### Step 4. 先试”识别不变”的调参

> 📝 推荐先问：
> 1. **你选择的变换是否改变了 x / y 的经济含义？**
>    改变了 = 这不是”识别不变”，你在偷换变量。
> 2. **如果变换让结果”显著”了，但代价是经济含义变弱，你会选哪一个？**
>    默认选”显著” = 已经在 p-hacking。
> 3. **这一步能不能保证：换了任何一种变换，研究问题不变？**
>    不能 = 你在做”找替代指标”，而不是”识别不变调参”。
>    完整追问清单见 [`references/socratic-prompts.md`](references/socratic-prompts.md#step-4--先试识别不变的调参)。

在不更换核心识别含义的前提下，先对当前指标做变换与清理尝试。

常见顺序：

1. 保持原口径
2. `ln(x)` 或 `ln(y)`（仅限严格正值）
3. `ln(x+1)` 或 `ln(y+1)`（适合含零非负变量）
4. `asinh(x)` 或 `asinh(y)`（适合含零、长尾、金额类）
5. 合理缩尾
6. 比例化、标准化或单位重定义
7. 在变量性质要求下改成二值、计数或占比处理
8. 合理分子/分母重构

常见变换菜单见 `references/transform-playbook.md`。

### Step 5. 再试模型层面的调参

> 📝 推荐先问：
> 1. **你的事件研究与主回归"绑在一起审"了吗？它们是一致的吗？**
>    没审 = 你跳过了模型层最关键的一致性体检。
> 2. **如果不一致，你会怎么解释这种不一致？还是会调整识别让它一致？**
>    答"调识别让它一致" = 已经是事后合理化。
> 3. **这一步聚类层级 / FE 结构 / 事件窗口的选择有没有"为了显著而选"的成分？**
>    有 = 应该先回 Step 2 重审数据。
>    完整追问清单见 [`references/socratic-prompts.md`](references/socratic-prompts.md#step-5--再试模型层面的调参)。

包括：

- 固定效应结构
- 聚类层级
- 交错 DID / 堆叠 DID / 其他可比模型
- 事件窗口
- 事件研究图与主回归是否一致

这一层非常关键：**回归结果和事件研究图必须一起看**。  
如果回归结果和事件研究图不一致，先停下来解释，不要继续堆规格。

### Step 5.5 先排查”样本问题”和”吸收问题”

> 📝 推荐先问：
> 1. **加控制变量后样本缩了多少？缩的是哪些观察？**
>    不清楚 = 你不知道结果变好是因为加对了控制，还是换了一批人。
> 2. **高维 FE 是否可能吸收了核心 x 的识别？**
>    不知道 = 默认假设”没问题”，但默认假设是经验研究最大的坑。
> 3. **这一步的目标是不是”先解释为什么之前不显著”，而不是”马上让结果显著”？**
>    后者 = 顺序错了，先回 Step 5 而不是接着做 Step 6。
>    完整追问清单见 [`references/socratic-prompts.md`](references/socratic-prompts.md#step-55--先排查样本问题和吸收问题)。

在把“不显著”解释成变量口径问题之前，先检查两件事：

1. 控制变量是否导致样本大幅收缩
2. 高维固定效应是否直接吸收核心解释变量

如果出现以下情况，必须先做审计，再继续搜索：

- 匹配控制变量后样本县数或观测值明显下降
- 完整案例删除后，样本从“多数样本”缩到“小半样本”
- 加入城市×年份、行业×年份、地区×年份等高维固定效应后，核心变量被 `omitted`

这一步的目标不是直接得到更显著结果，而是先回答：

- 当前结果不理想，到底是因为口径问题、样本问题，还是固定效应吸收问题？

### Step 6. 控制变量放在后面，不放在前面

> 📝 推荐先问：
> 1. **这一组控制变量的经济学理由是什么？是调节变量还是混淆变量？**
>    答不出 = 你是在"加很多变量看哪个让结果稳"。
> 2. **如果加了控制变量让结果反向，你怎么解释？是样本问题还是识别问题？**
>    答不上来 = 你在做"事后挑控制变量"。
> 3. **你加控制的顺序是"低缺失 → 强含义 → 完整"吗？每一层加完你都审计样本缩了多少吗？**
>    没审计 = 你没法解释"为什么加这层之后结果变了"。
>    完整追问清单见 [`references/socratic-prompts.md`](references/socratic-prompts.md#step-6--控制变量放在后面不放在前面)。

控制变量通常影响的是：

- 精度
- 显著性强弱
- 样本是否收缩

它们**不应成为主要的“扭转符号工具”**。  
如果一加控制变量，符号就大幅翻转，优先解释：

- 样本是否变了
- 分母是否变了
- 高维固定效应是否吸收了识别

只有在前面的 `x/y/样本/模型/事件研究` 都过了一遍之后，才系统上控制变量。

### Step 7. 当前指标失败后，才考虑替代指标

> 📝 推荐先问：
> 1. **这个替代指标和原指标在文献里通常怎么区分？**
>    不知道 = 你在"凭空造新指标"，这是 v2.0.0 硬边界明确禁止的。
> 2. **如果你做了替代指标，需要在正文里专门讨论"为什么放弃原指标"吗？**
>    不讨论 = 审稿人会问"是不是因为原指标不显著才换的"。
> 3. **这一步是不是真的"原指标合理变换都失败"之后才进入？**
>    不是 = 顺序错了，你跳过了中间轮次。
>    完整追问清单见 [`references/socratic-prompts.md`](references/socratic-prompts.md#step-7--当前指标失败后才考虑替代指标)。

只有在当前指标的合理变换都失败后，才进入“同概念替代指标”阶段。

这一阶段只做两件事：

1. 提供替代思路，例如金额改比例、流量改强度、总量改人均、应计改实缴。
2. 明确告诉用户：如果需要具体替代指标，必须补文献、变量表或已有数据字典。

不要在没有依据时，自行命名或创造具体替代指标。

### Step 8. 最终输出与停机

> 📝 推荐先问：
> 1. **你推荐的规格是"最显著"还是"最能辩护"？**
>    默认前者 = 已经在 p-hacking 的最后一步。
> 2. **如果审稿人质疑"你为什么不是用那条更显著的规格"，你能怎么回答？**
>    答不上来 = 你没准备好辩护你推荐的规格。
> 3. **停机是基于"已经够了"还是"已经累了"？**
>    后者 = 不是停机，是放弃。
>    完整追问清单见 [`references/socratic-prompts.md`](references/socratic-prompts.md#step-8--最终输出与停机)。

最终必须同时输出两部分：

1. 推荐规格
2. 完整实验日志

推荐规格的排序标准是：

1. 经济含义清楚
2. 与研究故事一致
3. 数据口径与制度背景可辩护
4. 方向稳定
5. 识别和模型匹配
6. 显著性更好

不要把“最显著”自动等同于“最该采用”。

如果任务目标是投稿或正文改写，最终还必须补一段“正文采用建议”，说明：

1. 哪个规格适合写入正文
2. 哪些规格只适合作为探索或附录
3. 哪些规格虽然更显著，但不建议采用

## 输出格式
默认按以下结构汇报：

1. 当前处于模式 A 还是模式 B
2. 当前锁定的 `x`、`y` 与方法
3. 这轮主要试的是哪一边
4. 已尝试规格列表
5. 推荐保留规格
6. 不推荐规格及原因
7. 为什么停
8. 如果还要继续，需要用户补什么材料

## 执行提醒
当任务涉及真实代码和回归时：

- 先读现有 do 文件和既有结果，不要脱离项目随意设计。
- 尽量把每一轮搜索独立成单独 do 文件或日志，避免污染主回归链条。
- 如果用户已经在正文使用某种模型，优先把搜索限制在该模型附近。
- 若发现结果方向和故事根本冲突，应直说，不要机械继续“试到显著”。
- 若正文已存在表格顺序或口径命名，输出文件应尽量贴合正文顺序，避免形成“跑出来了但无法贴回论文”的结果。
- 每一轮 Stata 搜索都应让使用者事后能回答五个问题：
  - 试了什么
  - 为什么试这个
  - 为什么停
  - 正文为什么选这一列
  - 哪些结果虽然显著，但不能用

## 投稿实战规则

当用户明确说这轮结果要进入正文、投稿附件、附录或供审稿人复核时，执行以下额外规则：

1. 优先可复现
   - do 文件名称清楚
   - 输出文件独立
   - 日志路径可追踪
2. 优先可解释
   - 选择正文口径时先看经济含义，再看显著性
3. 优先可贴回论文
   - 结果表和口径命名尽量与正文表述一致
4. 优先保留失败记录
   - 失败尝试不一定写进正文，但应保留在日志或探索 do 中
5. 不要让“探索 do”污染“正式 do”
   - 正式采用的结果应有单独、干净、可复跑的版本

## 常见卡点与降级处理

遇到以下情况时，按顺序降级而不是硬推：

1. 当前指标方向对但不显著
   - 先试变换
   - 再试轻量样本限制
   - 再试变量再表达
   - 最后才考虑替代指标
2. 当前指标方向错
   - 先检查经济含义和清洗逻辑
   - 若方向持续与故事冲突，应建议放弃该路径
3. 新规格更显著但解释变差
   - 可以保留为补充结果，但不自动升格为正文主规格
4. 只有边缘显著
   - 明确标记为边缘显著，不得夸大
5. 只有异常短窗口、异常小样本或异常窄定义才显著
   - 记录，但不自动升格为推荐规格
6. 题目本身已失去制度可发表性
   - 建议封存，不再继续调参

## 实战示例

需要快速理解如何落地时，读取 `references/case-examples.md`。

## 新增参考文件

本次 2.0.0 版本新增了三类参考：

- 搜索顺序：`references/attempt-ladder.md`
- 停机规则：`references/stop-rules.md`
- Stata 协作输出规范：`references/stata-collab-template.md`

本次 2.0.1 版本新增了一份配套参考：

- Step 0 机械可执行清单：`references/step0-checklist.md` ⭐（与 SKILL.md 的 Step 0 段同步；包含 4 层动作清单 + 中国常见官方源速查表 + step0-audit.md 输出模板）

本次 2.0.2 版本新增了一份过程质量门：

- 苏格拉底式提问协议（完整清单）：`references/socratic-prompts.md` ⭐（与 SKILL.md 每个 Step 之前的"📝 推荐先问"3 问同步；含追问、对应失败模式、推荐留痕位置）

## 需要按需读取的参考文件

- 选择方法时读：`references/method-menu.md`
- 选择变换时读：`references/transform-playbook.md`
- 输出实验记录时读：`references/spec-log-template.md`
- 需要和用户确认模式时读：`references/mode-selection.md`
- 涉及 Stata 代码、结果表、日志和投稿附件时读：`references/stata-output-conventions.md`
- 想看真实案例如何套用时读：`references/case-examples.md`
- 想按顺序执行显著性搜索时读：`references/attempt-ladder.md`
- 想判断什么时候该停时读：`references/stop-rules.md`
- 需要把结果嵌入双代理 Stata 协作流程时读：`references/stata-collab-template.md`
- 跑 7 轮顺序**之前**先读：`references/step0-checklist.md`（机械可执行的 4 层 pre-flight 清单）
- 每个 Step 之前要回答的关键问题清单：`references/socratic-prompts.md`（完整版，每 Step 含 6-10 个追问 + 失败模式映射）
